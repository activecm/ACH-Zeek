# Don't commit any generated resources
*.tar.gz
*.tar

# These scripts are tracked in the zeek-log-transport repo.
# We download them into the staging directory,
# but do not want to commit them here.
scripts/install_data_import.sh
scripts/zeek_log_transport.sh

# These scripts are tracked in the docker-zeek repo.
scripts/zeek

# These scripts are tracked in separate Zeek package repos.
zeek_scripts/site/zeek_open_connections.zeek

# These files are autogenerated for each build
VERSION
